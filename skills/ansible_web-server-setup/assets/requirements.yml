---
# requirements.yml -- Collection dependencies for web-server-setup
# Generated by AnsibleClaw (compose)
#
# Install with: ansible-galaxy collection install -r assets/requirements.yml
#
collections:
  - name: ansible.posix
