---
# playbook.yml -- Ready-to-use playbook for ansible.builtin.package
# Generated by AnsibleClaw
#
- name: "Install abc package"
  hosts: all
  become: true
  tasks:
    - name: Install abc package
      ansible.builtin.package:
        name: abc
        state: present
